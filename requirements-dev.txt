# أدوات التطوير فقط — لا تثبّتها في الإنتاج!
# للتثبيت: pip install -r requirements-dev.txt

-r requirements.txt

# Debug
flask-debugtoolbar==0.14.1

# Testing
pytest>=7.0
pytest-flask>=1.2

# Linting
flake8>=6.0
black>=23.0
